# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# error-handling
- Wrap user-facing operations (registration, login, form submission, image uploading) in try-catch blocks with user-friendly error handling to prevent unexpected errors from panicking users. Confidence: 0.70

# filesystem
- When Linux case-sensitivity issues arise between code (uppercase paths like `Config/`, `Core/`, `Views/`) and on-disk directories (lowercase), rename/move directories to match the code rather than modifying code. Confidence: 0.70

# git
- Always push code to remote after making major changes. Confidence: 0.85

